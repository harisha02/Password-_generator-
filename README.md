# Password-_generator-
Simple Password Generator built using Python. It creates a secure random password based on the user’s chosen length. The program uses random and string modules to include letters, numbers, and special characters. It also handles invalid input using exception handling.
